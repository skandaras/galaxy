<div class="stub">
	<h2>Admin</h2>
	<p>
		The panel UI lands in M2 (with the Observatory). The admin API is already live:
		providers, model sync &amp; enablement, task configs, and web-search/compaction settings —
		see <code>/api/admin/*</code>.
	</p>
</div>

<style>
	.stub {
		margin: auto;
		text-align: center;
		color: var(--fg-dim);
		font-size: 0.85rem;
		max-width: 34rem;
	}
	h2 {
		color: var(--fg);
		letter-spacing: 0.2em;
	}
	code {
		color: var(--accent);
	}
</style>
